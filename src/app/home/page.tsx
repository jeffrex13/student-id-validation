import ScannerContainer from '@/components/ScannerContainer';
import React from 'react';

const HomePage = () => {
  return (
    <div className="container mx-auto pt-40 max-w-xl">
      <ScannerContainer />
    </div>
  );
};

export default HomePage;
